fn render(title: String, count: Int) -> Int {
    print(title);
    let mut i: Int = 0;
    while (i < count) {
        print(i);
        i = i + 1;
    }
    return i;
}

let frames: Int = render("donut", 3);
frames;
